interface:
  display_name: "Erstan Work Manager"
  short_description: "Manage Erstan work and collaboration"
  default_prompt: "Use $erstan-work-manager to inspect or update the requested Erstan work items safely."
dependencies:
  tools:
    - type: "mcp"
      value: "erstan-beta"
      description: "Erstan hosted MCP server"
      transport: "streamable_http"
      url: "https://api-qa.erstan.com/v1/mcp"
policy:
  allow_implicit_invocation: true
